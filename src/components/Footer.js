/* eslint-disable react/jsx-no-comment-textnodes */
import { LogoText, Nav, NavItem, Footer } from "./Components";
import scrollToSection from "../utils/Utils";

const FooterSection = () => {
  return (
    <Footer>
      <LogoText>saifalim_</LogoText>
    </Footer>
  );
};

export default FooterSection;
